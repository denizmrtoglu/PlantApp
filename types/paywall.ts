export interface FeatureItem {
  id: number;
  title: string;
  subtitle: string;
  icon: string;
}

export interface SubscriptionItem {
  id: number;
  title: string;
  subtitle: string;
  badge?: string;
}
